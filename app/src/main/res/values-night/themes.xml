<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Modo escuro: força tema claro pois o app não tem dark mode -->
    <style name="Theme.Alarmed" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/green_500</item>
        <item name="colorPrimaryVariant">@color/green_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/green_200</item>
        <item name="colorSecondaryVariant">@color/green_700</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Background color. -->
        <item name="android:windowBackground">@color/gray_100</item>
        <!-- Surface and text colors: garante texto escuro em fundos claros -->
        <item name="colorSurface">@color/white</item>
        <item name="colorOnSurface">@color/gray_900</item>
        <item name="android:editTextColor">@color/gray_900</item>
        <item name="android:textColorHint">@color/gray_400</item>
    </style>
</resources>